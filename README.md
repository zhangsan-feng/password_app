
```
    这是一个自用的密码管理的 app 
    目前仅支持局域网同步账号状态 pc 和 移动端
    
    flutter pub get
    flutter build windows --release ; flutter build apk --release --target-platform android-arm64;
    

    项目说明在project.md 
```

构建产物：

- Windows：`build/windows/x64/runner/Release/password_app.exe`
- Android：`build/app/outputs/flutter-apk/app-release.apk`
